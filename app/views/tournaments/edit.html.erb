<h1>Editing Tournament</h1>

<%= render 'form', tournament: @tournament %>

<%= form_tag generate_tournament_path(@tournament) do %>
    <%= submit_tag 'Generate bracket' %>
<% end %>

<%= link_to 'Show', @tournament %> |
<%= link_to 'Back', tournaments_path %>
